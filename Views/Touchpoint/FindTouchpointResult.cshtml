@inherits System.Web.Mvc.WebViewPage<IEnumerable<Generic.view_TouchpointData>>
@using System.Web.Mvc.Html
@using Generic.Helpers
@using Telerik.Web.Mvc.UI;
@{
    Layout = LayoutHelper.GetLayout();
    ViewBag.Title = "Touchpoint";
}

<style>
    div#TouchpointGrid
    {
        font-size: 11px;
    }

    #modal
    {
        height: 489px;
    }

    .ui-dialog
    {
        width: 99% !important;
        left: 0;
    }
</style>

@( Html.Telerik().Grid(Model).Name("TouchpointGrid")
.ToolBar(commands => commands
                .Custom()
                    .HtmlAttributes(new { id = "export" })
                    .Text("Export to Excel")
                    .Action("ExportExcel", "Touchpoint"))

        .Columns(columns =>
        {
            columns.Bound(o => o.name).Title("Name").Width(150).Template(@<text><a href='#' id='@item.name' class="menu_block">@item.name</a>
<div class="display_menu" style="display: none;">   <ul>
        <li><a href="javascript:openEditPopup(@item.id)">Edit/Correct</a></li>
        <li><a href="javascript:openRemovePopup(@item.id,'@item.name')">Remove</a></li>
        <li><a href="javascript:openArchivePopup(@item.id,'@item.name')">Archive</a></li>
        <li><a href="javascript:openViewDetailPopup(@item.id)">View Detail</a></li>
        <li><a href="~/TouchPoint/EventNotification/@item.id">Event Notification</a></li>
        
    </ul>
</div>
</text>);

            columns.Bound(o => o.Start_Date).Title("Start Date").Width(100);
            columns.Bound(o => o.End_Date).Width(100);
            columns.Bound(o => o.Locked).Title("Locked").Width(100);
            columns.Bound(o => o.Group_Count).Title("Group Count").Width(100);
            columns.Bound(o => o.Partner_Count).Title("Partner Count").Width(100);

            columns.Bound(o => o.Questionnaire_Count).Title("Questionnaire Count").Width(100);
            columns.Bound(o => o.description).Title("Description").Width(100);
            columns.Bound(o => o.Touchpoint_Admin).Title("Touchpoint Admin").Width(100);
            columns.Bound(o => o.Touchpoint_Sponsor).Title("Touchpoint Sponsor").Width(100);
            columns.Bound(o => o.User_Count).Title("User Count").Width(100);
            

        })
        .DataKeys(keys => keys.Add(o => o.id))
        .Pageable(pager => pager.PageSize(13).Total(200).Enabled(true))
        .Sortable(sorting => sorting.Enabled(true))

        .Scrollable(scrolling => scrolling
           .Enabled(true)
           .Height(380))
           .HtmlAttributes(new { @class = "ra" })



)
<div id="EmailDialog" title="Questionnaire Routing Form">
    <p>Email dialog</p>
</div>
<div id="genericPopup" title=""></div>
<style>
    .t-grid-content tr > td:hover .menu_block + .display_menu {
        display: block !important;
    }

    .display_menu {
        left: 65px;
        overflow: visible;
        position: absolute;
        top: -5px;
        background: #fff;
        border: 1px solid #ddd;
        z-index: 100;
    }

    .t-grid-content tr > td {
        overflow: visible !important;
        position: relative;
    }

    .display_menu > ul {
        padding: 0 16px;
    }

    .display_menu li {
        list-style: none outside none;
        width: 143px;
    }
</style>
<script src="~/Scripts/jquery-1.8.2.js"></script>
<script src="~/Scripts/jquery-ui-1.9.2.js"></script>
<script type="text/javascript">
    $(function () {
        jQuery.noConflict();
        $("#EmailDialog").dialog({
            autoOpen: false
        });
        $('#popup').bind('dialogclose', function (event) {
            window.location.replace(window.location.href);
        });
    });
    function CloseMe() {
        $("#genericPopup").dialog('close');
        //$(".t-icon .t-refresh").trigger('click');

        window.location.replace(window.location.href);
    }

    function OpenEmailDialog(ac) {
        jQuery.noConflict();

        $.get("/WrongContact/Index", { accessCode: ac }, function (data) {

            $("#EmailDialog").html(data);
            $("#EmailDialog").dialog({
                width: 1200,
                height: 1000,
            });
            $("#EmailDialog").dialog('open');
        });
    }
    $('#menu_block').hover(function () {
        // alert("call");
        $('#display_menu').show();
    }, function () {
        $('#display_menu').hide();
    })

    $('#menu_block').mouseenter(function () {
        $('#display_menu').show();
    }).mouseleave(function () {

        $('#display_menu').hide();

    });

    function openEditPopup(id) {
        $.get("@Url.Content("~/Touchpoint/Edit/")" + id, function (data) {
            $("#genericPopup").html(data);
        });

        $("#genericPopup").dialog({ height: 600, title: "Edit/Correct" });
        $("#genericPopup").dialog('open');
    }

    function openRemovePopup(id,  name) {



        alertify.set({
            labels: {
                ok: "No",
                cancel: "Yes"
            },
            buttonFocus: "cancel"
        });
        // button labels will be "Accept" and "Deny"
        alertify.confirm("You are about to Remove  : " + name + ". Are you sure you want to do this?", function (e) {
            if (e) {
                // Click no
            } else {
                // click yes

                $.get("@Url.Content("~/Touchpoint/Archive/")" + id, function (data) {
                    // $(".t-icon .t-refresh").trigger('click');
                    window.location.replace(window.location.href);
                });

            }
        });


    }


    function openArchivePopup(id,  name) {



        alertify.set({
            labels: {
                ok: "No",
                cancel: "Yes"
            },
            buttonFocus: "cancel"
        });
        // button labels will be "Accept" and "Deny"
        alertify.confirm("You are about to Archive : " + name + ". Are you sure you want to do this?", function (e) {
            if (e) {
                // Click no
            } else {
                // click yes

                $.get("@Url.Content("~/Touchpoint/Archive/")" + id, function (data) {
                    // $(".t-icon .t-refresh").trigger('click');
                    window.location.replace(window.location.href);
                });

            }
        });


    }

    function openViewDetailPopup(id) {
        $("#genericPopup").html("<p>Loading Data...</p>");
        $.get("@Url.Content("~/Touchpoint/Details/")" + id, function (data) {
            $("#genericPopup").html(data);
        });
        $("#genericPopup").dialog({ height: 600, title: "View Detail" });
        $("#genericPopup").dialog('open');
    }


</script>

